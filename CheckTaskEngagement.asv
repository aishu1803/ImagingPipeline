function p_baselinecheck = CheckTaskEngagement(trials)
re = [trials.reward;];
re_ind = find(re);
trials = trials(re_ind);
for i =1:size(trials(1).C_raw,1)
    tmp_baseline = []; tmp_np=[];
    for j = 1:length(trials)
        tmp_baseline = [tmp_baseline trials(j).C_raw(i,trials(j).nosepokeframe-fps)
    end
end
end